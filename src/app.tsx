import * as React from "react";
import * as ReactDOM from "react-dom";
import { PageTest } from "./pages/pageTest/PageTest";

function render() {
  ReactDOM.render(<PageTest />, document.body);
}

render();
